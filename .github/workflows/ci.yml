name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # Allows manual triggering

jobs:
  build_and_test_docker:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build Docker image
      run: docker-compose -f docker/docker-compose.yml build dev

    - name: Lint with Ruff
      run: docker-compose -f docker/docker-compose.yml run --rm dev uv run ruff check .

    - name: Check formatting with Ruff
      run: docker-compose -f docker/docker-compose.yml run --rm dev uv run ruff format --check .

    - name: Run tests
      run: docker-compose -f docker/docker-compose.yml run --rm dev uv run python -m unittest discover -s tests -p 'test_*.py'
